steps:
- checkout: self
  submodules: recursive

- task: NodeTool@0
  displayName: 'Use node v18.12'
  inputs:
    versionSpec: '18.12'

- task: Cache@2
  displayName: 'Cache Yarn'
  inputs:
    key: 'yarn | "$(Agent.OS)" | package-lock.json'
    restoreKeys: |
      yarn | "$(Agent.OS)"
    path: $(YarnCachePath)

- script: yarn install --cache-folder $(YarnCachePath)
  displayName: 'Install Dependencies'
